
import { Category, Product } from "@/types";
import { sampleCategories, sampleProducts } from "@/data/sampleData";

// Export the shared data sources
export const categories: Category[] = sampleCategories;
export const products: Product[] = sampleProducts;
