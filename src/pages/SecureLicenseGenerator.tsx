
import React from "react";
import SecureKeyGenerator from "@/features/license/admin/SecureKeyGenerator";

const SecureLicenseGenerator: React.FC = () => {
  return <SecureKeyGenerator />;
};

export default SecureLicenseGenerator;
